{
  "name": "challenge",
  "version": "1.0.0",
  "description": "testing",
  "main": "App.js",
  "scripts": {
    "test": "jest --collectCoverage=false",
    "test:name": "jest --collectCoverage=false name"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "jest": "^24.9.0"
  }
}
